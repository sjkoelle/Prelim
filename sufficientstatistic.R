sufficientstatistic = function(x, family){
  
  
  
  if(family == "Poisson"){
   # ss = function(x){
      return(x)
    #}
  }
  if(family == "Gaussian"){
    #ss = function(x){
      return(x^2)
   # }
  }
  if(family == "exponential"){
    #ss = function(x){
      return(x)
    #}
  }
  
 # return(ss)
}
